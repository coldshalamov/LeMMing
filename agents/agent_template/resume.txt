Name: agent_template
Role: Replace with agent title
Description: Single-sentence summary of what this agent does.
Workflow: Outline how the agent processes inputs and produces outputs in simple steps.

[INSTRUCTIONS]
Write clear, action-oriented steps that match the JSON resume. State what to read, how to reason, and what to write.
Keep instructions concise so future edits are easy to diff. Avoid assuming tools beyond those listed in permissions.

[CONFIG]
{
  "model": {
    "key": "gpt-4.1-mini",
    "temperature": 0.2,
    "max_tokens": 2048
  },
  "permissions": {
    "read_outboxes": ["*"],
    "tools": ["memory_read", "memory_write"]
  },
  "schedule": {
    "run_every_n_ticks": 1,
    "phase_offset": 0
  },
  "credits": {
    "max_credits": 500.0,
    "soft_cap": 250.0
  }
}
