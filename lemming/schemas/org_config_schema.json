{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LeMMing Org Config",
  "type": "object",
  "additionalProperties": false,
  "required": ["base_turn_seconds"],
  "properties": {
    "base_turn_seconds": {"type": "number", "exclusiveMinimum": 0},
    "max_turns": {"type": ["integer", "null"], "minimum": 1},
    "summary_every_n_turns": {"type": "integer", "minimum": 1},
    "max_outbox_age_ticks": {"type": "integer", "minimum": 1}
  }
}
